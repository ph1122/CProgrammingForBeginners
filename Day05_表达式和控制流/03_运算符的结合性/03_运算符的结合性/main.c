#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

// 只有单目运算符 | ~ - + ++ -- * & sizeof
// 条件运算符 ?:
// 赋值运算符 = += -= *= /= %= <<= >>= &= ^= |=
// 从右到左
int main() {
    // 单目、条件、赋值  右结合性
  // 单目：~~~~~~a;从右往左
  // 条件：第6章再讲
  // 赋值
    int a = 1, b = 2, c = 3;
    a += b -= c *= 6;
    // a += (b -= (c *= 6));
    // a += (b -= 18);
    // a += -16;
    // c = 18, b = -16, a = -15 
    printf("%d %d %d\n", a, b, c); // -15 -16 18

    a = b = c = 18;



	return 0;
}